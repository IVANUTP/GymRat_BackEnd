CREATE TABLE tbl_tipo_rutina(
                                id_tipo_rutina BIGSERIAL PRIMARY KEY,
                                nombre VARCHAR(100) NOT NULL,
                                descripcion TEXT
);